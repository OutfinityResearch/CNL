--- CONTEXT: Agency ---

// Action performance
Rule: If X performs Y and Y causes Z, then X affects Z.

// Intention and performance
Rule: If X intends Y and X performs Y, then Y is intentional.

// Causation transitivity
Rule: If X causes Y and Y causes Z, then X causes Z.

// Agency and effects
Rule: If X is an agent and X performs Y, then X affects Y.