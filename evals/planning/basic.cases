# Planning Evaluation Cases

- input: "Package1 is a package.\nPackage1 is packed.\nEvery package that is packed is shipped.\nEvery package that is shipped is delivered.\nPlan to achieve every package is delivered."
  purpose: "Plan command using multi-step derived goal."
  expect: "satisfied"
