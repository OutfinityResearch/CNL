# evals/parsing/basic.cases

## Purpose
End-to-end parsing cases with expected AST summaries or hashes.

## Notes
- Keep inputs representative of relative clauses and grouping rules.
- Prefer the canonical corpus in `evals/parsing/cnl-pl-parser.v1.json` for baseline coverage.
